{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run server",
            "type": "shell",
            "command": "python3 -m http.server"
        },
        {
            "label": "run server on windows",
            "type": "shell",
            "command": "source ~/.bashrc; winpty python.exe -m http.server"
        }, // 
        {
            "label": "start my_sql server",
            "type": "shell",
            "command": "mysql -u root -p"
        },
        {
            "label": "reset database",
            "type": "shell",
            "command": "source my_sql/equipment_loan.sql"
        }
    ]
}